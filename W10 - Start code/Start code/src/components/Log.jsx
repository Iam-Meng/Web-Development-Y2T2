
function Log({ isPlayer, isDamage, text }) {
    return (
        <section id="log" className="container">
            <h2>Battle Log</h2>
            
        </section>
    );
}

export default Log;
